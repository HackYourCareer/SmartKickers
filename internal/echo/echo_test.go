package echo

import (
	"testing"
)

func Test_sendScore(t *testing.T) {
	////con := connect()
	////type args struct {
	////	c         *websocket.Conn
	////	gameScore messages.GameScore
	////}
	////tests := []struct {
	////	name string
	////	args args
	////}{
	////	{"test1", {"", messages.GameScore{1, 2}}},
	////}
	////for _, tt := range tests {
	////	t.Run(tt.name, func(t *testing.T) {
	////		sendScore(tt.args.c, tt.args.gameScore)
	////	})
	////}
	//s := httptest.NewServer(http.HandlerFunc(sendScore()))
}
